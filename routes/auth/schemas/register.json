{
  "type": "object",
  "$id": "schema:auth:register",
  "required": ["username", "password", "email"],
  "properties": {
    "username": {
      "$ref": "schema:auth:username#"
    },
    "password": {
      "$ref": "schema:auth:password#"
    },
    "email": {
      "$ref": "schema:auth:email#"
    }
  }
}
