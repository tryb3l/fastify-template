{
  "type": "object",
  "$id": "schema:auth:register",
  "required": ["username", "email", "password"],
  "properties": {
    "username": {
      "$ref": "schema:username#"
    },
    "password": {
      "$ref": "schema:password#"
    },
    "email": {
      "$ref": "schema:email#"
    }
  }
}
